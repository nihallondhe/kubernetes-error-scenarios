apiVersion: v1
kind: Pod
metadata:
  name: pod-bad-crashloop
  namespace: three-tier-app
spec:
  containers:
    - name: app
      image: busybox:1.36
      command: ["sh", "-c", "echo 'starting'; sleep 2; exit 1"]
