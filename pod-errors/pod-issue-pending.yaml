apiVersion: v1
kind: Pod
metadata:
  name: pod-bad-pending
  namespace: three-tier-app
spec:
  containers:
    - name: app
      image: nginx:alpine
      resources:
        requests:
          cpu: "100"
          memory: "200Gi"
        limits:
          cpu: "100"
          memory: "200Gi"
